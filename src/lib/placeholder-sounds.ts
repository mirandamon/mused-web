// src/lib/placeholder-sounds.ts
import type { Sound } from './types';

// Preset sounds are removed as they are now fetched dynamically via the API.
// Keeping the file structure in case other static sound definitions are needed later.

// marketplaceSounds are fetched from the API via SoundSelectionSheet
// export const marketplaceSounds: Sound[] = [ ... ]; // Removed
